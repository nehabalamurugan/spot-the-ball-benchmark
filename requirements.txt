We provide all the code used to generate the data, and reproduce the results in the paper.
The larger dataset is too large for github so we have it on huggingface, but the evaluation dataset is included here. 

Setup 
1. Install the dependencies
```
pip install -r requirements.txt
```